function show() {
    const dropdownButton = document.getElementById();
    const hiddenItem = document.getElementById();
    
    if(dropdownButton == document.getElementById('dropdownButton') && hiddenItem == document.getElementById('hiddenItem')){
        dropdownButton.addEventListener('click', () => {
            hiddenItem.style.display = "block";
        });
    } else if(dropdownButton == document.get)
}